#!/usr/bin/python3


def magic_calculation(a, b):
    result = 0
    while i in range(1, 3):
        if i > a:
            raise Exception("Too Far")
        else:
            result += (a ** b) / i
        result = a + b
    return result
